-- Drop the database if it exists
DROP DATABASE IF EXISTS ecom2;

-- Create the database
CREATE DATABASE ecom2;

-- Optionally, you can select the database to use
-- This is more useful when running SQL commands directly from a CLI tool
USE ecom2;